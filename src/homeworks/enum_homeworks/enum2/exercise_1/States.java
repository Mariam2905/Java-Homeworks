package homeworks.enum_homeworks.enum2.exercise_1;


/**
 * Implement a traffic light using an abstract class and an enum in Java.
 * The traffic light should have three states: RED, YELLOW, and GREEN,
 * and it should transition between these states automatically every few seconds.
 * The abstract class should define a method called changeState() that the enum
 * can use to transition to the next state.
 */
public enum States {
    RED, YELLOW, GREEN;


}
